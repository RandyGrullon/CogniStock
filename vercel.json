{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "comment": "El proyecto Vercel debe configurarse con Root Directory = 'frontend'. La configuración real (crons, etc.) vive en frontend/vercel.json."
}
